var active_index;

$('#myTable tr   ').each(function(i) {
   var body=Number($('.todo-id',this).text());
   var comp= Number('<%= @todo.id%>');
   console.log(body);
   console.log(comp);
   if (body==comp){
     // console.log($(".active",this))
    // $(" .active",this).removeClass("btn-circle");
    $(".active",this).toggleClass("btn-circle-inactive");
    active_index=i;
  }
  last=i;
});
$('#myTable tr ').each(function (i) {
  $('.up-link', this).css('visibility','visible');
  $('.down-link', this).css('visibility','visible');
  if (i==0) {
    $('.up-link', this).css('visibility','hidden');
  }
  last=i;
});
$('#myTable tr ').each(function(i) {
  if (i==last){
      $('.down-link',this).css('visibility','hidden');
    }
  });
